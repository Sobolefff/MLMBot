# TODO — Greenway Bot MVP

Список задач и доработок, приоритизированный. Обновляется по ходу разработки согласно дорожной карте (`greenway_bot_mvp_prompt.md`).

Легенда приоритетов: 🔴 критично (блокирует MVP) · 🟡 важно (нужно для полноценного MVP) · 🟢 желательно (можно отложить на Phase 2+)

---

## 🔴 Критично

- [ ] Парсер каталога товаров Greenway (цена/PV/категория для PV-Подборщика) — разведка 24.09.2026 (`pyapi.greenwaystart.com`) покрыла бизнес-аналитику партнёра (команда/финансы/PRO-бонус), но НЕ публичный каталог товаров; источник каталога всё ещё нужно найти отдельно (возможно тоже JSON API — стоит проверить `Мой бизнес → Ссылки`/раздел заказа товаров в личном кабинете тем же методом: DevTools → Network)
- [ ] Досмотреть в DevTools недостающие эндпоинты pyapi (см. память `reference-greenway-pyapi`, раздел «НЕ подтверждены»): эндпоинт логина, эндпоинт рефреша токена, «Мои заказы» (потенциальный источник для таблицы `sales`), «Мои счета»/«Выплаты», «Стейтмент»
- [ ] Cron job для ежедневного обновления каталога товаров (после того как найден источник)

## 🟡 Важно

### Интеграция с pyapi.greenwaystart.com (бизнес-аналитика партнёра — новое направление поверх исходного MVP-скоупа)

- [ ] Механизм получения токена: либо (а) реверс-инжиниринг эндпоинта логина pyapi (нужно доснять руками), либо (б) Playwright-флоу логина на greenwayglobal.com с извлечением cookie `accessToken`/`pyapi_refresh_token` — решить, когда будет ясен эндпоинт логина/степень защиты (капча и т.п.)
- [ ] Сервис синхронизации: раз в день дёргать `client.getMainView(gwPartnerId)` для каждого активного `greenway_accounts`, писать снимок в `greenway_snapshots`, обновлять `token_expires_at`/`last_synced_at`
- [ ] Обработка истечения токена (`GreenwayClient` уже бросает ошибку с `code: 'TOKEN_EXPIRED'`) — авто-рефреш через `pyapi_refresh_token`, когда эндпоинт рефреша будет найден
- [ ] Алерты в боте: новая активация/регистрация в структуре, партнёр стал `is_terminated`, просадка СГО/ЛГО > X% к прошлому периоду (поле `sub_value_percent` уже готово в API)
- [ ] Еженедельный/ежемесячный дайджест партнёру: рост команды + динамика объёма + прогресс до следующего звания (шкалы `pro-bonus`) + статус окна выплат
- [ ] Перед включением автополлинга — свериться с пользовательским соглашением Greenway насчёт автоматизированного доступа; закладывать разумную частоту опроса (не чаще раз в час), не расшаривать данные структуры (там ПДн третьих лиц — телефоны/email/адреса других партнёров) без их согласия

### Прочее

- [ ] Автоматическое обновление `clients.last_order_date` / `total_pv` при поступлении новых продаж (зависит от источника данных о продажах — см. «Мои заказы» выше)
- [ ] Deeplink на реферальную ссылку партнёра в сообщениях клиенту (сценарий 1 user flow клиента)
- [ ] Персонализированные рекомендации в цепочках (сейчас топ-3 товара по PV для всех, без учёта категорий клиента — нужна таблица позиций заказа, которой пока нет в схеме)
- [ ] `bull` тянет уязвимую версию `uuid` (moderate, GHSA-w5hq-g745-h8pq) — фикс требует мажорного апгрейда `bull` (breaking change для очередей уведомлений), нужно тестировать отдельно перед апгрейдом

## 🟢 Желательно / Phase 2+

- [ ] Рейтинги/клубы из pyapi (Президентский совет, Grand/Masters/Leaders Club) — уведомление о новом звании у себя или в структуре
- [ ] Гео-аналитика структуры по городам/странам (`countries-cities`) — визуализация роста команды
- [ ] Экспорт снепшотов метрик в Excel/Google Sheets (данные уже структурированы из pyapi JSON, не нужно парсить готовый «Скачать Excel» с сайта)

- [ ] AI-рекомендации товаров на основе истории клиента
- [ ] Gamification (баллы лояльности, уровни)
- [ ] Личная витрина партнёра
- [ ] Live Chat партнёр ↔ клиент через бота
- [ ] 2FA для партнёров
- [ ] Экспорт отчётов, dashboard аналитики
- [ ] Мультиязычность
- [ ] Отдельный (более строгий) rate limit на `POST /partners/:id/clients`
- [ ] Ротация JWT-секрета / отзыв токенов (blacklist или refresh-токены) — сейчас токен живёт 7 дней без принудительного logout
- [ ] Явно настроить `helmet` под чистый JSON API (например, отключить `contentSecurityPolicy`/`crossOriginEmbedderPolicy`, если не появится сторонних интеграций) — сейчас включён с дефолтными настройками, для API это безопасно, но не оптимально

---

## Выполнено

- [x] Схема БД (partners, clients, products, sales, deadlines, chains, notifications, audit_logs)
- [x] PV-Подборщик: DP-алгоритм, до 3 комбинаций, < 500ms, unit-тесты
- [x] REST API: auth, partners, pv-calc, chains, deadlines
- [x] Telegram-бот: регистрация с согласием по ФЗ-152, главное меню, PV-Подборщик, цепочки, сроки
- [x] Уведомления по дедлайнам через Bull Queue (-72ч/-24ч/-2ч/0ч) + воркер отправки
- [x] Audit log для действий партнёров
- [x] Docker/docker-compose заготовка (app, bot, worker, redis)
- [x] Rate limiting на /auth (express-rate-limit)
- [x] Обработчик настроек партнёра в боте (⚙️ Настройки): профиль, тумблеры уведомлений/цепочек, добавление клиента, удаление всех данных (ФЗ-152)
- [x] Хендлер активации/деактивации цепочки и удаления клиента из бота (инлайн-кнопки в «🔔 Мои цепочки»)
- [x] Cron-планировщик цепочек (`src/scheduler/chainsCron.js`, ежедневно) + `chainsService.checkAndNotifyChains` реально проверяет `last_order_date` и ставит уведомления в очередь `chain-notifications`
- [x] Воркер бота теперь обрабатывает обе очереди уведомлений (`deadline-notifications` и `chain-notifications`)
- [x] API для удаления клиента и полного удаления данных партнёра (`DELETE /partners/:id`, право на удаление по ФЗ-152)
- [x] Backup-скрипт для SQLite (`scripts/backup-db.sh`, ежедневно, retention по `BACKUP_RETENTION_DAYS`)
- [x] Docker healthcheck для `app` (`curl /health`) и `redis` (`redis-cli ping`) в `docker/docker-compose.yml`
- [x] Пример конфига Nginx + инструкция по SSL (Let's Encrypt/certbot) — `docker/nginx.conf.example`, README раздел «Деплой»
- [x] E2E-тесты Telegram-бота (`tests/e2e/bot.e2e.test.js`, 15 тестов: регистрация, PV-Подборщик, цепочки, настройки, удаление данных)
- [x] Security review: добавлены `helmet`, CORS-белый список (`ALLOWED_ORIGINS`), лимит размера тела запроса, проверка `JWT_SECRET` при старте в проде (`assertProductionSecrets`), rate limiting расширен на `/pv-calc/search` и `/partners/:id/settings`/`DELETE`, логирование `ctx.update` в боте очищено от ПДн, удалена неиспользуемая зависимость `bcrypt` (заодно убрала критическую уязвимость в её транзитивной цепочке); SQL injection — аудит пройден, везде параметризованные запросы
- [x] Стартовая архитектура интеграции с pyapi.greenwaystart.com: `src/greenway/tokenCrypto.js` (AES-256-GCM шифрование токенов, ключ из `GREENWAY_TOKEN_ENC_KEY`), `src/greenway/client.js` (обёртка над подтверждёнными эндпоинтами — main-view, partner/list, pro-bonus, dynamic-structure, sgo-dynamics, registrations/activations, countries-cities — с троттлингом и ретраями), таблицы `greenway_accounts`/`greenway_snapshots` в схеме БД, unit-тесты (13 новых)
